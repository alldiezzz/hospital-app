= form_for @booking, url: booking_create_path do |f|
  = f.label 'Doctor Name'
  = @booking.doctor.name
  = f.hidden_field :doctor_id
  %br
  = f.label 'Booking Time'
  = f.datetime_select :booking_time, start_year: Time.now.year
  %br
  = f.submit